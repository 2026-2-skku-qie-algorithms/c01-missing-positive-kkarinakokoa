# Design Brief

AI 도구를 열기 전에 작성하세요(파이프라인 1~3단계). 인터뷰 이후에는 수정하지 마세요. 이 브리프와 최종 spec의 차이는 spec(docs/specs/)의 5번 섹션에 작성합니다.

## 1. Problem, restated

입력: 최대 100,000개의 정수로 이루어진 리스트

출력: 리스트에 존재하지 않는 가장 작은 양의 정수 (즉 가장 작은 자연수)

제약 조건 및 가정: 리스트는 비어 있을 수 있으며, 비어 있는 경우 결과는 1이다. 리스트에는 0, 음수, 중복된 값이 포함될 수 있다. 양의 정수만 결과에 영향을 준다.

## 2. Edge cases with expected outputs (at least three)

각 사례를 한 줄로 작성하세요: 입력, 예상 출력, 그리고 해당 사례가 중요한 이유.

- 입력: [] | 예상 출력: 1 | 이유: 빈 입력의 경우를 테스트한다.
- 입력: [1, 2, 3] | 예상 출력: 4 | 이유: 1부터 리스트 길이까지의 모든 양의 정수가 존재하는 경우를 테스트한다.
- 입력: [0, -1, -2] | 예상 출력: 1 | 이유: 0과 음수가 결과에 영향을 주지 않는지 테스트한다.
- 입력: [1, 1, 2, 2] | 예상 출력: 3 | 이유: 중복된 값이 결과에 영향을 주지 않는지 테스트한다.
- 입력: [2, 3, 4] | 예상 출력: 1 | 이유: 1이 누락된 경우를 테스트한다.
- 입력: [2, 4, 1, 6, 5, 3] | 예상 출력: 7 | 이유: 입력 리스트가 정렬되어 있을 필요가 없는지 테스트한다.
- 입력: [1, 2, 5, 6] | 예상 출력: 3 | 이유: 가장 작고 리스트엔 나오지 않은 양의 정수가 입력의 최댓값보다 작을 수 있는 경우를 테스트한다.

## 3. Algorithm

상태(각 변수와 해당 변수의 초기값):

- positive_values: 입력에서 양의 정수들을 저장할 빈 set (집합).
- _candidate: 1에서 시작하며, 현재 확인 중인 양의 정수를 나타낸다.

단계(1, 2, 3과 같이 번호를 매기고, 각 단계의 하위 단계는 a, b, c로 작성하세요. 각 단계에는 하나의 구체적인 동작만 포함하고, 무엇을 무엇과 비교하는지와 반복문이 언제 종료되는지 명시하세요):

1. 입력 리스트를 원소 하나씩 첫 원소부터 순회한다.
   a. 각 값을 0과 비교한다.
   b. 값이 0보다 크면 positive_values에 추가한다.
   c. 입력 리스트의 모든 원소를 확인하면 종료한다.
2. _candidate = 1에서 시작하여, _candidate가 positive_values에 존재하는지 확인한다.
   a. 존재한다면 _candidate를 1 증가시킨다.
   b. 증가된 _candidate 값이 positive_values에 존재하는지 다시 확인한다.
   c. _candidate가 positive_values에 존재하지 않을 때 종료한다.
3. _candidate를 답으로 반환한다.

## 4. Trace on one small input

입력: [3, 4, -1, 1]

알고리즘의 각 단계마다, 해당 단계 이후의 상태 변수 값을 한 줄씩 작성하세요.

- 시작: positive_values = {}, _candidate = 1
- 1단계 이후: positive_values = {1, 3, 4}, _candidate = 1
- 2단계 이후: 1이 존재하므로 _candidate = 2가 된다. 2는 존재하지 않으므로 Loop를 종료한다.
- 3단계 이후: 2를 반환한다.

결과: 2

예상 출력과 일치하는가? 예

## 5. Expected cost

Time complexity (시간 복잡도)를 쓰고 왜 그렇게 생각하는지도 한 문장으로 써주세요: O(n). 알고리즘은 입력을 한 번 순회한 뒤 set에서 연속된 양의 정수의 존재 여부를 확인하며, 각 membership check의 expected Time complexity는 O(1)이기 때문이다.

Extra space (추가 필요한 메모리 공간): O(n). set에 입력의 모든 양의 값(최악의 경우 입력 리스트의 값들 전체)이 저장될 수 있기 때문이다.

이 알고리즘은 문제의 입력 크기 제한에 대해 충분히 빠른가요? 그리고 그 이유는 무엇인가요?

- 예. 입력 리스트의 길이가 최대 100,000 이므로, O(n) 알고리즘은 입력 크기에 비례하는 작업만 수행하며 이 제한을 효율적으로 처리할 수 있다.
